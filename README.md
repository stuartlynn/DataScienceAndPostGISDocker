# Data science notebook + postgis environment

Simple env for doing data science stuff with postgis

Simply do

```bash
docker-compose up
```

To add any python deps, modify the Dockerfile to pip install then
